#include "../inc/push_swap.h"

void			conquer_three(t_node **stack)
{
	t_node		top;
	t_node		second;
	t_node		third;

	top = stack->prev;
	second = top->prev;
	third = second->prev;

	if (top < second < third)
	
	else if (top < second < third)
	else if (top < second < third)
	else if (top < second < third)
	else if (top < second < third)

}

void			sort_under_six()
{

}